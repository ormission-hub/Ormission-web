"use client";

/**
 * UserWatermark (Deprecated & Disabled)
 * 
 * Floating watermark has been permanently disabled per user instructions.
 * This component returns null to ensure zero overlay distraction.
 */
export function UserWatermark() {
  return null;
}
